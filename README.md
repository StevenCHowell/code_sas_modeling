Code for "Considerations for Atomistic Modeling of Small-Angle Scattering Data"

This code can be installed for development use by running the command
```
pip install -e .
```
or for normal use by running the command
```
pip install .
```